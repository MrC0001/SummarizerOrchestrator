# .env

# Backend API base URL (replace <LOAD_BALANCER_IP> and <PORT> with actual values)
# API_BASE_URL=http://<LOAD_BALANCER_IP>:<PORT>

# Port for the frontend server
PORT=3000

# Node environment (development or production)
NODE_ENV=development